<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/chat"
    app:startDestination="@id/chatFragment2">
    <fragment
        android:id="@+id/chatFragment2"
        android:name="com.sesameware.smartyard_oem.ui.main.chat.ChatFragment"
        android:label="ChatFragment" >
        <action
            android:id="@+id/action_chatFragment2_to_customWebViewFragment2"
            app:destination="@id/customWebViewFragment2" />
    </fragment>
    <fragment
        android:id="@+id/customWebViewFragment2"
        android:name="com.sesameware.smartyard_oem.ui.custom_web_view.CustomWebViewFragment"
        android:label="fragment_custom_web_view"
        tools:layout="@layout/fragment_custom_web_view" >
        <argument
            android:name="basePath"
            app:argType="string"
            app:nullable="true" />
        <argument
            android:name="code"
            app:argType="string"
            app:nullable="true" />
        <argument
            android:name="title"
            app:argType="string"
            app:nullable="true" />
        <argument
            android:name="hasBackButton"
            app:argType="boolean"
            android:defaultValue="true" />
    </fragment>
</navigation>