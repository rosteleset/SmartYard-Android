<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:context=".ui.custom_web_view.CustomWebBottomFragment">

    <ImageView
        android:id="@+id/ivExtBottomBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="8dp"
        android:contentDescription="@string/web_bottom_back"
        app:srcCompat="@drawable/ic_dialog_bottom_sheet_rectangle" />

    <FrameLayout
        android:id="@+id/wvContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false">

        <WebView
            android:id="@+id/wvExtBottom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:layout_height="200dp"
            android:layout_margin="8dp"
            android:isScrollContainer="true"
            android:nestedScrollingEnabled="true" />

        <ProgressBar
            android:id="@+id/pbWebViewBottom"
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:indeterminateTint="@color/brand"
            android:visibility="invisible"
            tools:visibility="visible" />

    </FrameLayout>

</LinearLayout>
